extends layout

block content
    form(method='POST', action='')
        div.form-group
            label(for='note') Note:
            textarea#note.form-control(placeholder='New note here!' name='note', rows=5, cols=80)
        button.btn.btn-primary(type='submit') Create

    if errors
        ul
            for error in errors
                li!= error.msg
